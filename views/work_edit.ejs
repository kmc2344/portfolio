<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>Work Edit</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body class="admin-body">

<div class="admin-container">

  <h1 class="admin-title">作品を編集</h1>

  <div class="edit-card">

    <form action="/admin/work/<%= work.id %>/edit" method="POST" class="edit-form">

      <div class="edit-section">
        <label>タイトル</label>
        <input type="text" name="title" value="<%= work.title %>" required>
      </div>

      <div class="edit-section">
        <label>説明</label>
        <textarea name="desc" rows="4" required><%= work.desc %></textarea>
      </div>

      <div class="edit-section">
        <label>現在の画像</label>
        <img src="<%= work.image %>" class="edit-preview">
      </div>

      <div class="edit-section">
        <label>画像パス（Git管理）</label>
        <input type="text" name="image" value="<%= work.image %>" required>
        <small style="opacity:0.7; line-height:1.5;">
          例：/assets/works/sample.jpg（public/assets に配置）
        </small>
      </div>

      <div class="edit-actions">
        <a href="/admin" class="edit-back">← 戻る</a>
        <button type="submit" class="submit-btn">保存する</button>
      </div>

    </form>

  </div>

</div>

</body>
</html>
